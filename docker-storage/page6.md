### Volumes use the host filesystem

When writing to a Docker volume - you are writing to the **host**.

This means you can use any type of storage device and filesystem that the host is configured to use.