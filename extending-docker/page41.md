#### we can now use the docker cli

 * this means orchestration tools can now use them
 * it also means the 2 adapters can be combined
 * a single container can be orchestrated with storage AND networking